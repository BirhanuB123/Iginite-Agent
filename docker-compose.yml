services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: adwa
      POSTGRES_PASSWORD: adwa
      POSTGRES_DB: adwa_agent
    ports:
      - "5432:5432"
    volumes:
      - adwa_db:/var/lib/postgresql/data
volumes:
  adwa_db:
