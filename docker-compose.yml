services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ignite
      POSTGRES_PASSWORD: ignite
      POSTGRES_DB: ignite_agent
    ports:
      - "5432:5432"
    volumes:
      - ignite_db:/var/lib/postgresql/data
volumes:
  ignite_db:
